server:
  port: 7001
  error:
    include-message: always
logging:
  level:
    root: INFO
    nl.svh.microservices: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
spring:
  data:
    mongodb:
      auto-index-creation: true
      host: localhost
      port: 27017
      database: product-db
---
spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      host: mongodb
server:
  port: 8080
